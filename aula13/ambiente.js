num = [2, 4, 5, 6, 8, 4, 8, 7]

let pos = num.indexOf(8);

console.log(`${pos}`);

/* 
for (let pos = 0; pos < num.length; pos++) {
    console.log(`A posição ${pos+1} tem ${num[pos]} como valor.`);
} */

/* for (let pos in num) {
    console.log(`A posição ${Number(pos)+1} tem ${num[pos]} como valor.`);
} */